# KFOO Evidence Regions V1

This layer creates immutable crops from explicitly configured normalized image regions. It does not claim that a crop contains a KFOO signal.

No default screen coordinates are supplied because the repository does not establish a stable KFOO layout. A captured region remains `UNREADABLE` until a semantic verifier can prove its contents.

Execution remains OFF.
